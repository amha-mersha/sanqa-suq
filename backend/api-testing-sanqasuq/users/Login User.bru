meta {
  name: Login User
  type: http
  seq: 2
}

post {
  url: {{user_url}}/login
  body: json
  auth: inherit
}

body:json {
  // UserLoginDTO
  {
    "email": "john.doe@example.com",
    "password": "secure12345"
  }
}
